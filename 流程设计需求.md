### 背景
* 对公客户经理需要一个尽调报告的智能问答助手，目标是推荐尽调报告

### 具体流程
用户数据输入 -> 意图识别 -> 案例推荐 -> 提取用户信息中的标签信息 -> 

无标签则多轮问询并提供问题样例给用户/有标签

有标签 -> 根据标签与数据库中的标签进行匹配查询 -> 拿到尽调报告数据返回给前端

### 数据
数据输入案例可以参考data/尽调报告综述与标签_简化示例.json

### 你需要干什么
* 实现流程中功能
* 评判哪些大模型框架能实现，代码语言也需要确认
* 数据为 尽调报告综述文档与标签数据
* 你是作为后端，需要明确与前端的通信方式, 入参与出参
* 明确返回给前端的通信格式
* 通过什么方案起服务能让前端拿到数据
* 能减少依赖则不扩充，轻量化设计