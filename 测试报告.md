# 尽调报告助手统一对话测试报告

## 1. 测试结论

| 项目 | 结果 |
|---|---|
| 测试日期 | 2026-08-10 |
| 数据集 | 8 份报告、160 个标签 |
| Python 自动化测试 | 41 项通过 |
| Vue Vitest | 4 项通过 |
| TypeScript / Vite 构建 | 通过，兼容 Chrome 130 |
| Playwright 真实浏览器 | 通过，控制台 0 错误/0 警告 |
| DeepSeek 真实接口 | 四类意图及推荐/筛选多轮通过 |
| Docker | 按要求未测试 |

总体结论：案例推荐、多维筛选、数据统计和比赛问答已统一收敛到
`POST /api/v1/chat`。动态建议、意图切换、历史保存、运营查询及页面展示均达到本期验收要求。

## 2. 后端自动化测试

执行命令：

```powershell
python -m pytest -m "not live" -q
```

结果：`41 passed`，退出码 0。

主要覆盖：

- 启动时完整重建报告及标签；会话、消息和模型配置跨重启保留。
- 模型结构化意图识别、解析失败关键词兜底、明确切换流程和混合会话标识。
- 标签累计、歧义确认、跳过、按现有信息生成和最多五轮追问。
- 无条件推荐稳定排序、Top 3、金额区间、短文本包含及关联案例排除自身。
- 多维筛选 AND 条件和无条件完整度排序。
- 报告数、标签数、行业、报告类型及标签分布由 SQLite 聚合计算。
- 比赛回答调用活动模型并固定附加非官方资料免责声明。
- “猜你想问”每批 3 条且意图不重复，首批默认推荐/筛选/统计，换批优先补入比赛问答；历史和模型失败场景可回退。
- 运营端按消息实际意图检索，混合会话仍可被其中任一功能筛选命中。
- OpenAPI 和路由中已删除四个旧接口，无兼容层。
- API Key Fernet 加密、掩码输出、测试后激活和安全失败。

pytest 在 Windows 解释器退出阶段出现一次 pytest 临时目录清理权限提示；测试本身已全部通过且退出码为 0，不属于应用故障。

## 3. 前端测试与生产构建

```powershell
cd frontend
npm test -- --run
npm run build
```

- Vitest：`4/4 passed`。
- TypeScript 类型检查：通过。
- Vite 生产构建：通过；JavaScript 114.69 KB，CSS 14.31 KB（未压缩）。
- 覆盖结构化建议展示/发送、推荐关联操作、运营端风险提示和密钥掩码。
- 无外部 CDN、在线字体或在线图片。

## 4. Playwright 真实浏览器验收

在本机服务 `http://127.0.0.1:8010` 上验证：

- 375×812 移动首页正常展示 3 条带功能标识的建议和四个功能入口。
- 点击功能入口后在消息区展示输入案例、助手回复案例和快捷问题。
- 点击“换一批”后，上一批缺少的比赛问答被优先补入，文本无重复。
- 在统一底部输入框发送“现在有多少篇报告？”，返回“报告总数 = 1”的数据库统计卡。
- 运营概览正确展示四类功能使用次数、活动模型和最近会话。
- 对话历史展示识别意图并可按推荐、筛选、统计、比赛问答和混合会话过滤。
- 浏览器控制台：0 错误、0 警告。

截图：

- `output/playwright/unified-mobile.png`
- `output/playwright/unified-ops.png`

## 5. DeepSeek 真实接口测试

活动模型：`deepseek-v4-pro`（OpenAI Chat Completions 兼容接口）。

| POST 输入 | 识别意图 | 结果 |
|---|---|---|
| `给我推荐几个优秀的报告` | recommendation | 追问行业；输入“按现有信息生成”后返回 1 条推荐，得分 72.0 |
| `筛选科学研究和技术服务业的报告` | filter | 追问主营业务；输入“直接筛选”后返回 1 条筛选结果 |
| `现在有多少篇报告？` | statistics | 返回数据库统计值 1 |
| `比赛要求是什么？` | qa | 返回模型回答，并包含“未接入官方比赛资料，仅供参考”免责声明 |

当前部署健康检查状态为 ready，报告数 8、标签数 160。首批建议为推荐/筛选/统计；携带上一批批次标识换批后为比赛问答/推荐/筛选。

## 6. 接口与安全边界

已确认 OpenAPI 不再包含：

- `GET /api/v1/filter-options`
- `POST /api/v1/reports/search`
- `GET /api/v1/statistics/reports`
- `POST /api/v1/qa/mock`

其他边界：

- `.env`、SQLite 运行文件和 `node_modules` 不进入版本控制。
- API Key 不通过接口、页面、日志或前端构建产物暴露。
- 运营端继续明确提示“当前运营端无访问鉴权”，仅限可信内网。
- 比赛问答未接入 RAG，不作为官方事实来源。
- 报告数据只在服务启动时从 `data/**/*.json` 加载，运行期间保持不变。
- Docker 按用户要求未测试。
