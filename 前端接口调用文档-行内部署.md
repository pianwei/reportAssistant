# 尽调报告助手前端接口调用文档（行内部署）

本文档面向行内前端开发和联调人员，依据当前后端代码整理。后端为 FastAPI 服务，所有业务接口均使用 JSON，不使用 SSE/WebSocket，也没有文件上传接口。

## 1. 接入约定

### 1.1 服务地址

当前行内部署默认监听 `8720` 端口，接口统一前缀为：

```text
http://<行内服务器IP或域名>:8720/api/v1
```

推荐由行内网关做同源反向代理，前端只访问相对路径：

```text
/api/v1
```

若前后端跨域部署，后端环境变量 `CORS_ORIGINS` 必须包含前端页面的完整 Origin，例如：

```dotenv
CORS_ORIGINS=https://due-diligence.example.bank
```

Origin 只包含协议、域名和端口，不带路径。当前后端跨域仅允许请求头 `Content-Type`、`X-Request-ID`。如果行内网关要求浏览器发送 `Authorization` 等认证头，需要由网关处理 CORS，或同步调整后端允许的请求头。

### 1.2 鉴权边界（重要）

当前应用业务接口本身**没有登录或 Token 校验**。配置项 `LLM_API_KEY` 和 `LLM_AUTH_HEADER` 仅供后端访问行内大模型，前端不得读取或传递大模型密钥。

生产部署建议：

- 行内统一身份认证、Token/Session 校验由可信网关完成；
- 网关认证后，将稳定且不可伪造的用户标识映射为业务 `user_id`；
- `/api/v1/ops/*` 仅开放给运营管理员；
- 不应将后端 `8720` 端口直接暴露给终端用户或非可信网络。

当前 `GET /conversations/{session_id}` 只按 `session_id` 查询，不再次核验用户归属。前端不可把它当作安全边界；正式上线应由网关限制访问，或后端补充身份校验。

### 1.3 通用请求头

```http
Content-Type: application/json
X-Request-ID: <可选，由前端或网关生成的链路ID>
```

不传 `X-Request-ID` 时后端会自动生成。每个响应都会返回：

```http
X-Request-ID: req_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

建议前端记录该值，报错时供日志排查。

### 1.4 用户与会话

- `user_id`：行内用户的稳定标识，长度 `1～64`。不要使用姓名、手机号等敏感明文；推荐使用员工号的受控映射值或网关下发的匿名标识。
- `session_id`：后端创建，格式类似 `ses_<32位字符>`，最大长度 `64`。
- 新会话：调用 `/chat` 时必须传 `session_id`，值传空字符串 `""` 或 `null`，同时必须传 `user_id`。
- 继续会话：传服务端返回的 `session_id`；建议仍同时传同一个 `user_id`，后端会校验会话归属。
- 新建会话：前端清空本地 `session_id`，下一次发消息时固定传 `user_id`、空的 `session_id` 和 `message`。

### 1.5 超时与重试

聊天、推荐词和模型测试接口可能调用大模型，不是流式响应。建议前端请求超时设为 `90 秒`，期间展示“处理中”。

仅在网络失败或错误体中 `retryable=true` 时提供重试。不要自动重试创建/修改/激活/删除模型配置等管理操作。

## 2. 接口总览

| 场景 | 方法 | 路径 | 前端权限建议 |
| --- | --- | --- | --- |
| 服务状态 | GET | `/health` | 普通用户/探针 |
| 页面初始化 | GET | `/ui/bootstrap` | 普通用户 |
| 统一对话 | POST | `/chat` | 普通用户 |
| 猜你想问 | POST | `/suggestions` | 普通用户 |
| 用户会话列表 | GET | `/users/{user_id}/conversations` | 本人 |
| 会话详情 | GET | `/conversations/{session_id}` | 本人 |
| 报告详情 | GET | `/reports/{report_id}` | 普通用户 |
| 运营会话列表/详情 | GET | `/ops/conversations[/{session_id}]` | 管理员 |
| 运营指标 | GET | `/ops/metrics` | 管理员 |
| 模型状态 | GET | `/ops/model-status` | 管理员 |
| 模型配置管理 | GET/POST/PATCH/DELETE | `/ops/model-profiles...` | 管理员 |

下文路径均省略前缀 `/api/v1`。

## 3. 普通用户接口

### 3.1 页面初始化

```http
GET /ui/bootstrap?user_id=<用户ID>
```

参数：

| 参数 | 位置 | 必填 | 说明 |
| --- | --- | --- | --- |
| `user_id` | Query | 是 | 当前用户标识 |

成功响应：

```json
{
  "user_id": "u_10001",
  "assistant": {
    "name": "尽调报告助手",
    "intro": "你好，我是尽调报告助手。推荐、筛选、统计和比赛问答都可以直接在这里对话。"
  },
  "feature_cards": [
    {
      "id": "recommendation",
      "title": "智能案例推荐",
      "icon": "spark",
      "description": "多轮理解需求，推荐相似尽调案例",
      "input_examples": ["给我推荐几个优秀的报告"],
      "assistant_example": "可以。你更关注客户行业、企业规模，还是授信方案？也可以跳过并直接推荐。"
    }
  ],
  "default_suggestions": [
    {"text": "给我推荐几个优秀的尽调报告", "intent": "recommendation"}
  ],
  "capabilities": {
    "rag_ready": false,
    "qa_source": "model"
  }
}
```

`feature_cards` 和 `default_suggestions` 的数量及文案由后端决定，前端应按数组动态渲染。

### 3.2 统一对话（核心接口）

```http
POST /chat
```

新建会话：

```json
{
  "user_id": "u_10001",
  "session_id": "",
  "message": "推荐适合科技型小微企业的尽调案例"
}
```

继续会话：

```json
{
  "user_id": "u_10001",
  "session_id": "ses_2be7f7a49abf4e53aee96bbac0bf1269",
  "message": "授信金额300万元，用于流动资金"
}
```

查看某报告的关联案例：

```json
{
  "user_id": "u_10001",
  "session_id": "ses_2be7f7a49abf4e53aee96bbac0bf1269",
  "action": {
    "type": "related_reports",
    "report_id": "rpt_0123456789abcdef01234567"
  }
}
```

请求字段：

| 字段 | 类型 | 必填 | 约束/说明 |
| --- | --- | --- | --- |
| `user_id` | string/null | 新会话必填 | `1～64` 字符；继续会话建议必传 |
| `session_id` | string/null | 是 | 固定必传；`""` 或 `null` 创建新会话，非空值继续已有会话 |
| `message` | string/null | 与 `action` 二选一 | 去除首尾空白后不能为空，最大 `10000` 字符 |
| `action` | object/null | 与 `message` 二选一 | 当前只支持 `related_reports` |

统一成功响应结构：

```json
{
  "request_id": "req_e429cf77ab06468d9b343f01366af55f",
  "session_id": "ses_2be7f7a49abf4e53aee96bbac0bf1269",
  "intent": "recommendation",
  "status": "recommendations",
  "assistant_message": "已根据当前信息推荐最匹配的3份尽调报告。",
  "collected_tags": [
    {
      "name": "行业分类",
      "value": "科学研究和技术服务业",
      "dimension": "客户维度",
      "confidence": 0.99
    }
  ],
  "information_incomplete": true,
  "question": null,
  "recommendations": [],
  "statistic": null,
  "answer": null
}
```

`recommendations` 始终为数组；没有尽调报告数据时返回 `[]`，不返回空字符串或 `null`。

`intent` 枚举：

| 值 | 含义 |
| --- | --- |
| `recommendation` | 推荐 Top 3 报告 |
| `filter` | 按全部条件筛选报告，结果可能较多 |
| `statistics` | 报告/标签数据统计 |
| `qa` | 比赛问答 |
| `greeting` | 问候 |
| `unsupported` | 超出能力范围 |

`status` 决定前端展示组件：

| `status` | 展示方式 | 主要数据字段 |
| --- | --- | --- |
| `recommendations` | 推荐报告卡片 | `recommendations` |
| `filter_results` | 筛选结果列表 | `recommendations` |
| `statistics` | 统计文本/图表/报告列表 | `statistic` |
| `answer` | 问答文本和免责声明 | `answer` |
| `greeting` | 普通助手消息 | `assistant_message` |
| `unsupported` | 能力边界提示 | `assistant_message` |
| `needs_clarification` | 补充信息提示 | `question` 可能为空，以 `assistant_message` 为准 |
| `error` | 预留值 | HTTP 错误当前使用统一错误体，不返回该状态 |

推荐项结构：

```json
{
  "report_id": "rpt_0123456789abcdef01234567",
  "report_name": "某科技企业授信业务调查报告",
  "report_type": "科技型企业",
  "score": 92.5,
  "recommendation_reason": "行业分类、企业规模等条件匹配。",
  "matched_tags": [
    {
      "name": "行业分类",
      "query_value": "科学研究和技术服务业",
      "report_value": "科学研究和技术服务业",
      "similarity": 1.0
    }
  ],
  "unmatched_tags": [],
  "missing_tags": ["授信品种"],
  "report_tags": [
    {"name": "行业分类", "value": "科学研究和技术服务业"}
  ],
  "summary": {
    "report_name": "某科技企业授信业务调查报告",
    "report_type": "科技型企业",
    "customer_overview": "……",
    "business_overview": "……",
    "financial_overview": "……",
    "credit_plan": "……",
    "guarantee_and_repayment": "……",
    "main_risks": ["……"],
    "overall_assessment": "……"
  }
}
```

注意：

- `score` 范围为 `0～100`，不要假定为整数；
- `summary` 目前是对象，前端应容忍服务端后续增加字段；
- `report_tags` 是用于卡片展示的简化标签；完整标签应调用报告详情接口；
- `recommendation` 固定最多返回 3 条，`filter` 可能返回全量匹配结果，前端应自行分页或虚拟滚动展示。

统计响应中的 `statistic`：

```json
{
  "metric": "industry_distribution",
  "title": "行业分布",
  "value": "制造业",
  "breakdown": [
    {"label": "制造业", "count": 85},
    {"label": "科技服务业", "count": 42}
  ],
  "reports": [
    {
      "report_id": "rpt_0123456789abcdef01234567",
      "report_name": "某企业尽调报告",
      "report_type": "制造业"
    }
  ]
}
```

`value` 可能是数字或字符串；`breakdown`、`reports` 可能为空数组。图表应先判断 `breakdown.length > 0`。

比赛问答响应中的 `answer`：

```json
{
  "content": "模型生成的回答内容",
  "source": "model",
  "official": false,
  "disclaimer": "当前未接入官方比赛资料，回答由大模型生成，仅供参考，请以主办方官方通知为准。"
}
```

前端必须展示 `disclaimer`，不得将 `official=false` 的内容标记为官方答案。

### 3.3 猜你想问

```http
POST /suggestions
```

请求：

```json
{
  "user_id": "u_10001",
  "session_id": "ses_2be7f7a49abf4e53aee96bbac0bf1269",
  "previous_batch_id": "sug_2b676e07d53a4395a2ca19b919624709"
}
```

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `user_id` | 是 | 用户标识 |
| `session_id` | 否 | 传入后会结合当前会话生成建议 |
| `previous_batch_id` | 否 | 上一批响应的 `batch_id`，用于减少重复 |

响应：

```json
{
  "suggestions": [
    {"text": "筛选制造业小微企业报告", "intent": "filter"},
    {"text": "现在有多少篇尽调报告？", "intent": "statistics"},
    {"text": "比赛评分标准是什么？", "intent": "qa"}
  ],
  "source": "model",
  "batch_id": "sug_2b676e07d53a4395a2ca19b919624709"
}
```

`source` 当前可能为 `model`、`fallback` 或 `qa_templates`。用户点击建议后，把 `text` 作为普通 `/chat` 的 `message` 发送即可。

### 3.4 用户会话列表

```http
GET /users/{user_id}/conversations?limit=20&cursor=<游标>
```

| 参数 | 必填 | 说明 |
| --- | --- | --- |
| `user_id` | 是 | Path 参数 |
| `limit` | 否 | 默认 20，后端最大按 100 条读取；前端建议 `1～100` |
| `cursor` | 否 | 使用上一页返回的 `next_cursor`，不要自行构造 |

响应：

```json
{
  "items": [
    {
      "session_id": "ses_...",
      "user_id": "u_10001",
      "title": "推荐适合科技型小微企业的报告",
      "feature": "recommendation",
      "active_intent": "recommendation",
      "created_at": "2026-08-14 09:30:00",
      "updated_at": "2026-08-14 09:31:15",
      "message_count": 4
    }
  ],
  "next_cursor": null
}
```

会话对象可能包含后端内部状态字段，前端只应依赖以上稳定展示字段。

### 3.5 会话详情

```http
GET /conversations/{session_id}
```

响应在会话对象基础上增加：

```json
{
  "session_id": "ses_...",
  "user_id": "u_10001",
  "title": "推荐适合科技型小微企业的报告",
  "feature": "recommendation",
  "created_at": "2026-08-14 09:30:00",
  "updated_at": "2026-08-14 09:31:15",
  "messages": [
    {
      "id": 1,
      "session_id": "ses_...",
      "role": "user",
      "content": "推荐适合科技型小微企业的报告",
      "message_type": "text",
      "request_id": "req_...",
      "payload": null,
      "intent": "recommendation",
      "created_at": "2026-08-14 09:30:00"
    },
    {
      "id": 2,
      "session_id": "ses_...",
      "role": "assistant",
      "content": "已根据当前信息推荐最匹配的3份尽调报告。",
      "message_type": "recommendations",
      "payload": {"status": "recommendations", "recommendations": []},
      "intent": "recommendation",
      "created_at": "2026-08-14 09:30:02"
    }
  ],
  "tags": [
    {"name": "行业分类", "value": "科学研究和技术服务业", "dimension": "客户维度", "confidence": 0.99}
  ]
}
```

恢复聊天 UI 时，气泡正文使用 `content`，报告卡片等结构化内容使用助手消息的 `payload`。旧数据或普通文本消息的 `payload` 可能为 `null`。

### 3.6 报告详情

```http
GET /reports/{report_id}
```

响应：

```json
{
  "report_id": "rpt_0123456789abcdef01234567",
  "report_name": "某科技企业授信业务调查报告",
  "report_type": "科技型企业",
  "summary": {
    "report_name": "某科技企业授信业务调查报告",
    "report_type": "科技型企业",
    "customer_overview": "……",
    "business_overview": "……",
    "financial_overview": "……",
    "credit_plan": "……",
    "guarantee_and_repayment": "……",
    "main_risks": ["风险一"],
    "overall_assessment": "……"
  },
  "source_file": "内部数据文件名.json",
  "tags": [
    {
      "dimension": "客户维度",
      "name": "行业分类",
      "value": "科学研究和技术服务业",
      "completeness": "是",
      "source_text": "报告原文片段",
      "note": ""
    }
  ]
}
```

`source_file` 和 `source_text` 可能包含内部信息。若前端用户无权查看，应在网关或后端脱敏，而不是仅在页面上隐藏。

## 4. 运营管理接口

所有 `/ops/*` 接口当前均无应用层鉴权，必须由行内网关限制为管理员访问。

### 4.1 会话查询

```http
GET /ops/conversations?user_id=&limit=20&cursor=&feature=&keyword=
GET /ops/conversations/{session_id}
```

`feature` 可用值：`recommendation`、`filter`、`statistics`、`qa`、`mixed`。列表和详情结构分别与普通用户的会话列表、会话详情一致。

### 4.2 运营指标

```http
GET /ops/metrics
```

响应：

```json
{
  "users": 120,
  "sessions": 560,
  "messages": 2310,
  "model_failures": 3,
  "feature_usage": {
    "recommendation": 320,
    "filter": 180,
    "statistics": 90,
    "qa": 40
  },
  "model": {
    "source": "environment",
    "profile_id": null,
    "profile_name": null,
    "model": "行内模型名称",
    "configured": true,
    "healthy": true
  },
  "recent_conversations": []
}
```

`feature_usage` 的键是动态的，前端不要假定所有分类一定存在。

### 4.3 模型状态

```http
GET /ops/model-status
```

响应即上节 `model` 对象。`healthy` 当前表示配置完整，不等同于实时探测成功；实际连通性应使用模型配置测试接口。

### 4.4 模型配置列表

```http
GET /ops/model-profiles
```

响应：

```json
{
  "items": [
    {
      "profile_id": "mdl_...",
      "name": "行内主模型",
      "provider": "OpenAI兼容",
      "base_url": "https://model.example.bank/v1",
      "model": "model-name",
      "timeout_seconds": 30.0,
      "json_mode": true,
      "disable_thinking": false,
      "is_active": true,
      "last_test_status": "success",
      "last_test_latency_ms": 850.4,
      "last_test_error": null,
      "last_test_at": "2026-08-14 09:20:00",
      "created_at": "2026-08-14 09:00:00",
      "updated_at": "2026-08-14 09:20:00",
      "api_key_masked": "sk-••••1234"
    }
  ],
  "master_key_configured": true
}
```

接口不会返回明文或密文 API Key。

### 4.5 新建模型配置

```http
POST /ops/model-profiles
```

请求：

```json
{
  "name": "行内备用模型",
  "provider": "OpenAI兼容",
  "base_url": "https://model.example.bank/v1",
  "model": "model-name",
  "api_key": "仅由管理员输入",
  "timeout_seconds": 30,
  "json_mode": true,
  "disable_thinking": false
}
```

成功状态码 `201`，响应为脱敏后的模型配置对象。API Key 需要可逆加密保存时，部署必须配置 `MODEL_CONFIG_MASTER_KEY`。

### 4.6 修改模型配置

```http
PATCH /ops/model-profiles/{profile_id}
```

只传需要修改的字段。激活中的配置不能直接修改；应新建配置、测试成功后再激活。

```json
{
  "timeout_seconds": 60,
  "api_key": "新的密钥"
}
```

密钥规则：

- 不传 `api_key` 或传空字符串：保留原密钥；
- `clear_api_key: true`：清除原密钥；
- 传非空 `api_key`：替换原密钥。

### 4.7 测试、激活和删除模型配置

```http
POST /ops/model-profiles/{profile_id}/test
POST /ops/model-profiles/{profile_id}/activate
DELETE /ops/model-profiles/{profile_id}
```

测试响应：

```json
{"success": true, "latency_ms": 812.35, "message": "连接和结构化输出正常"}
```

测试失败仍可能返回 HTTP `200`：

```json
{
  "success": false,
  "latency_ms": 30012.4,
  "message": "模型测试失败",
  "error_type": "ReadTimeout"
}
```

只有最近测试成功的配置才能激活。激活成功返回模型状态对象。激活中的配置不能删除；删除成功返回 HTTP `204`，无响应体。

## 5. 健康检查

```http
GET /health
```

响应：

```json
{
  "status": "ready",
  "data_loaded": true,
  "database": "ok",
  "model": "configured",
  "report_count": 293,
  "tag_count": 5860,
  "model_source": "environment"
}
```

`status=degraded` 时接口仍返回 HTTP `200`，前端应根据响应体显示服务未就绪。运维探针另有：

- `GET /health/live`：进程存活返回 `200`；
- `GET /health/ready`：数据或模型未就绪返回 `503`。

这两个探针未出现在 OpenAPI 文档中，但可供网关/Kubernetes 使用。

## 6. 统一错误处理

除模型测试接口的业务失败外，HTTP 错误统一返回：

```json
{
  "request_id": "req_2d5386c9dbaa4546a56dbc3f2d263f18",
  "error": {
    "code": "SESSION_NOT_FOUND",
    "message": "session_id 不存在",
    "retryable": false
  }
}
```

| HTTP 状态 | `error.code` | 常见原因 | 建议处理 |
| --- | --- | --- | --- |
| 400 | `MODEL_CONFIG_ERROR` | 模型配置非法、未测试即激活、修改活动配置等 | 展示 `message`，不要自动重试 |
| 404 | `SESSION_NOT_FOUND` | 会话不存在或服务端数据已重置 | 清除本地 `session_id`，提示新建会话 |
| 404 | `REPORT_NOT_FOUND` | 报告或关联报告不存在 | 刷新当前结果 |
| 404 | `NOT_FOUND` | 路径不存在/会话详情不存在 | 检查路径或资源状态 |
| 409 | `SESSION_USER_ERROR` | `session_id` 不属于传入的 `user_id` | 清除会话并重新登录/新建会话 |
| 422 | `INVALID_REQUEST` | JSON 字段、长度或类型校验失败 | 检查前端参数 |
| 422 | `SESSION_USER_ERROR` | 新会话未传 `user_id` | 补传用户标识 |
| 503 | `MODEL_UNAVAILABLE` | 模型未配置、超时或调用失败 | 可提示稍后重试；保留用户输入 |
| 500 | `INTERNAL_ERROR` | 未预期的服务端错误 | 展示通用提示并上报 `request_id` |

不要依赖 `message` 做程序判断，应使用 HTTP 状态和 `error.code`。只有 `retryable=true` 才建议用户重试。

## 7. 推荐的 TypeScript 调用封装

```ts
const API_BASE = import.meta.env.VITE_API_BASE_URL || '/api/v1'

export class ApiError extends Error {
  constructor(
    message: string,
    public status: number,
    public code: string,
    public requestId?: string,
    public retryable = false,
  ) {
    super(message)
  }
}

export async function api<T>(
  path: string,
  init: RequestInit = {},
  timeoutMs = 90_000,
): Promise<T> {
  const controller = new AbortController()
  const timer = window.setTimeout(() => controller.abort(), timeoutMs)
  const requestId = crypto.randomUUID()
  const headers = new Headers(init.headers)
  if (!headers.has('Content-Type')) headers.set('Content-Type', 'application/json')
  if (!headers.has('X-Request-ID')) headers.set('X-Request-ID', requestId)
  // 如行内网关另有认证要求，在这里按网关规范设置认证头。

  try {
    const response = await fetch(`${API_BASE}${path}`, {
      ...init,
      signal: controller.signal,
      headers,
    })

    const responseRequestId = response.headers.get('X-Request-ID') || requestId
    if (!response.ok) {
      const body = await response.json().catch(() => ({}))
      throw new ApiError(
        body?.error?.message || `请求失败（${response.status}）`,
        response.status,
        body?.error?.code || 'HTTP_ERROR',
        body?.request_id || responseRequestId,
        Boolean(body?.error?.retryable),
      )
    }

    if (response.status === 204) return undefined as T
    return response.json() as Promise<T>
  } finally {
    window.clearTimeout(timer)
  }
}

export const get = <T>(path: string) => api<T>(path)
export const post = <T>(path: string, body: unknown) =>
  api<T>(path, {method: 'POST', body: JSON.stringify(body)})
export const patch = <T>(path: string, body: unknown) =>
  api<T>(path, {method: 'PATCH', body: JSON.stringify(body)})
export const remove = (path: string) => api<void>(path, {method: 'DELETE'})
```

环境配置示例：

```dotenv
# 推荐：同源代理
VITE_API_BASE_URL=/api/v1

# 跨域直连（仅在 CORS 和网关策略允许时）
# VITE_API_BASE_URL=http://10.x.x.x:8720/api/v1
```

聊天调用：

```ts
interface ChatRequest {
  user_id?: string
  session_id?: string | null
  message?: string
  action?: {type: 'related_reports'; report_id: string}
}

const result = await post<any>('/chat', {
  user_id: currentUserId,
  session_id: currentSessionId,
  message: inputText,
} satisfies ChatRequest)

currentSessionId = result.session_id

switch (result.status) {
  case 'recommendations':
  case 'filter_results':
    renderReports(result.recommendations ?? [])
    break
  case 'statistics':
    renderStatistic(result.statistic)
    break
  case 'answer':
    renderAnswer(result.answer)
    break
  default:
    renderText(result.assistant_message)
}
```

## 8. 联调检查清单

1. `GET /health` 返回 `status=ready`、`data_loaded=true`、`model=configured`。
2. 新会话传 `user_id`，保存响应中的 `session_id`。
3. 同一会话后续请求始终使用同一个 `user_id`。
4. 根据 `status` 渲染结构化结果，不根据自然语言文案判断类型。
5. 展示比赛问答的 `disclaimer`。
6. 处理 `204` 空响应、`payload=null`、空数组和可空字段。
7. 记录响应头或错误体中的 `request_id`。
8. `503` 时保留用户输入并提供人工重试，不重复创建管理资源。
9. 网关分别验证普通用户接口和 `/ops/*` 管理员权限。
10. 验证会话越权、报告原文/源文件名暴露和跨域认证头策略。

FastAPI 自动接口页在未被网关关闭时可访问：

```text
http://<服务器>:8720/docs
http://<服务器>:8720/openapi.json
```

自动接口页用于开发联调，生产环境是否开放应服从行内安全规范。
